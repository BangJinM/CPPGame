
SET(FRAMEWORK_UTILS_HEADER 
Utils/SerializableHelper.h
)

SET(FRAMEWORK_UTILS_SRC 
Utils/SerializableHelper.cpp
)

list(APPEND FRAMEWORK_HEADER ${FRAMEWORK_UTILS_HEADER})
list(APPEND FRAMEWORK_SRC ${FRAMEWORK_UTILS_SRC})